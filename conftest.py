# Arquivo vazio de propósito: a presença dele faz o pytest adicionar a raiz
# do projeto ao sys.path, permitindo `from drinkgraph import ...` nos testes
# sem precisar instalar o pacote ou configurar PYTHONPATH manualmente.
